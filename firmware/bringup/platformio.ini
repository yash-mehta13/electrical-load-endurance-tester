[env:esp32dev]
platform = espressif32
board = esp32dev
framework = espidf
monitor_speed = 115200

; This project structure (CMakeLists.txt + idf_component_register in main/)
; is standard ESP-IDF and works as-is under PlatformIO's espidf framework —
; no firmware code changes needed, just this file.
